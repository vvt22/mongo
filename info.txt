firstly  : npm init    //going to initialze new node project for us
npm install express    // use express
//after all process
npm install mongodb


//after installing node mongodb driver 
connect to mongodb using the driver



find() explanation :
it returns a cursor (object points to set of document)
cursor has method like:toArray ,forEach
toArray fetches all document cursor points to put them in array
both methods gets the document in terms of batches  
... like forEach iterate through all element in one batch then next batch  



PAGINATION:-
//to limit the view to see documents
//using query parameter in the endpoint url
ex:www.localhost:3000/books/?p=1

INDEXS:-
scen:indexes allow database server to perform specific query to find document much more efficiently
WITHOUT NEEDING TO EXAMINE THE ENTIRE COLLECTION

.explain('executionStats')   //to see the statistics of the query
ex:   db.books.find({rating:8}).explain('executionStats')
.createIndex({})             //to create a index ..filter has 1(asc) -1(desc)
db.books.createIndex({rating: 1})   //here rating is taken as index
.getIndexes()                        //to get all indexes
.dropIndex({})                      //delete
